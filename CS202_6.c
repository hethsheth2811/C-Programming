#include <stdio.h>
#include <stdlib.h>
int main()
{
    /*Program to convert hours into minutes*/
    int hours;
    int minutes;
    printf("Enter the number of hours:");
    scanf("%d",&hours);
    printf("Conversion of hours into minutes:%d\n",minutes=hours*60);



    return 0;
}
